{
  "name": "ReSocketIO",
  "version": "0.0.1",
  "description": "socket.io in Reason",
  "scripts": {
    "postinstall": "npm run build",
    "test": "echo 'No tests yet.  All good :D",
    "build": "eval $(dependencyEnv) && nopam && rebel",
    "all": "npm run client && npm run server",
    "client": "rm -rf _build/top && mkdir -p _build/top && refmt -parse re -print ml common.re > _build/top/common.ml && refmt -parse re -print ml exampleclient.re > _build/top/exampleclient.ml && refmt -parse re -print ml src/client.re > _build/top/client.ml && refmt -parse re -print ml src/web.re > _build/top/web.ml && cd _build/top && bsc -bs-package-name ReSocketIO -c -bs-files *.ml && ../../node_modules/webpack/bin/webpack.js exampleclient.js --output-file app.js",
    "server": "rm -rf _build/server && rm -f server.js exampleserver.js namespace.js client.js common.js && mkdir -p _build/server && refmt -parse re -print ml common.re > _build/server/common.ml && refmt -parse re -print ml src/client.re > _build/server/client.ml && refmt -parse re -print ml src/namespace.re > _build/server/namespace.ml && refmt -parse re -print ml src/server.re > _build/server/server.ml && refmt -parse re -print ml exampleserver.re > _build/server/exampleserver.ml && cd _build/server && bsc -c -bs-files *.ml && cp exampleserver.js ../../exampleserver.js && cp server.js ../../server.js && cp namespace.js ../../namespace.js && cp client.js ../../client.js && cp common.js ../../common.js"
  },
  "dependencies": {
    "bs-platform": "git://github.com/bloomberg/bucklescript.git",
    "socket.io": "^1.4.8",
    "reason-js": "git://github.com/chenglou/reason-js.git",
    "@opam-alpha/merlin": "^ 2.5.0",
    "dependency-env": "https://github.com/npm-ml/dependency-env.git",
    "nopam": "https://github.com/yunxing/nopam.git",
    "rebel": "https://github.com/reasonml/rebel.git",
    "reason": "https://github.com/facebook/reason.git"
  },
   "rebel": {
    "targets": [
      {
        "target": "client",
        "engine": "bucklescript",
        "entry": "src/client.re"
      },
       {
        "target": "server",
        "engine": "bucklescript",
        "entry": "src/server.re"
      }
    ]
  },
  "devDependencies": {
    "express": "^4.14.0",
    "webpack": "^1.13.2"
  },
  "author": "Benjamin San Souci <benjamin.sansouci@gmail.com>"
}
